{
  "name": "@rheged-studio/markdownlint-config",
  "version": "3.0.1",
  "description": "Shared markdownlint configuration for consistent Markdown formatting",
  "keywords": [
    "markdownlint",
    "markdown",
    "linting",
    "formatting",
    "code-quality",
    "style-guide"
  ],
  "homepage": "https://github.com/rheged-studio/markdownlint-config#readme",
  "bugs": {
    "url": "https://github.com/rheged-studio/markdownlint-config/issues"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/rheged-studio/markdownlint-config.git"
  },
  "license": "MIT",
  "author": {
    "name": "Rob Easthope",
    "url": "https://github.com/RobEasthope"
  },
  "main": ".markdownlint.jsonc",
  "files": [
    ".markdownlint.jsonc",
    "README.md"
  ],
  "scripts": {
    "act:ci": "act pull_request -W .github/workflows/ci.yml --eventpath .github/act-events/pull_request.json",
    "act:list": "act --list",
    "act:release:dry": "act push -W .github/workflows/pkg-release.yml",
    "clean": "rimraf node_modules",
    "format": "npx prettier --write .",
    "lint": "pnpm run lint:md",
    "lint:md": "markdownlint-cli2 '**/*.{md,mdx}' '!**/node_modules/**' '!**/dist/**' '!**/.turbo/**' '!**/.astro/**' '!.agents/skills/**' '!.claude/skills/**'",
    "lint:md:fix": "markdownlint-cli2 --fix '**/*.{md,mdx}' '!**/node_modules/**' '!**/dist/**' '!**/.turbo/**' '!**/.astro/**' '!.agents/skills/**' '!.claude/skills/**'",
    "lint:sh": "bash -c 'if command -v shellcheck >/dev/null 2>&1; then shellcheck infrastructure/scripts/*.sh .husky/pre-commit .husky/pre-push .husky/commit-msg; elif [ \"$(uname -s)\" = \"Darwin\" ]; then echo \"⚠️  shellcheck not installed — skipping. Install: brew install shellcheck\"; else echo \"⚠️  shellcheck not installed — skipping. Install: apt-get install shellcheck\"; fi'",
    "lint:workflows": "actionlint",
    "lint:yaml": "yamllint .",
    "prepare": "husky",
    "release:manual": "npm publish --access public --provenance=false",
    "release:manual:dry": "npm publish --access public --provenance=false --dry-run",
    "sort-pkg-json": "sort-package-json",
    "test": "vitest run",
    "test:sh": "bash -c 'if command -v bats >/dev/null 2>&1; then bats infrastructure/tests/*.bats; elif [ \"$(uname -s)\" = \"Darwin\" ]; then echo \"⚠️  bats not installed — skipping. Install: brew install bats-core\"; else echo \"⚠️  bats not installed — skipping. Install: apt-get install bats\"; fi'",
    "test:watch": "vitest",
    "tsc": "tsc --noEmit",
    "validate:changelog": "pnpm exec changelog-core validate"
  },
  "lint-staged": {
    "**/*": [
      "prettier --write --ignore-unknown"
    ],
    "**/package.json": [
      "bash -c 'sort-package-json \"$@\" || true' --"
    ],
    "**/*.{md,mdx}": [
      "bash -c 'markdownlint-cli2 --fix --config ./.markdownlint-cli2.jsonc \"$@\" || true' --"
    ],
    "**/*.{yml,yaml}": [
      "bash -c 'if command -v yamllint >/dev/null 2>&1; then yamllint \"$@\"; elif [ \"$(uname -s)\" = \"Darwin\" ]; then echo \"⚠️  yamllint not installed — skipping. Install with: brew install yamllint\"; else echo \"⚠️  yamllint not installed — skipping. Install with: pip install --user yamllint==1.37.1\"; fi' --"
    ],
    ".github/workflows/*.{yml,yaml}": [
      "bash -c 'if command -v actionlint >/dev/null 2>&1; then actionlint \"$@\"; elif [ \"$(uname -s)\" = \"Darwin\" ]; then echo \"⚠️  actionlint not installed — skipping. Install with: brew install actionlint\"; else echo \"⚠️  actionlint not installed — skipping. Install via: bash <(curl -fsSL https://raw.githubusercontent.com/rhysd/actionlint/v1.7.5/scripts/download-actionlint.bash)\"; fi' --"
    ]
  },
  "devDependencies": {
    "@acme-skunkworks/changelog-core": "^1.1.1",
    "@acme-skunkworks/commitlint-config": "^1.0.1",
    "@commitlint/cli": "^21.2.1",
    "@types/node": "^25.6.0",
    "bats": "1.13.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.3.2",
    "markdownlint-cli2": "^0.22.1",
    "prettier": "^3.8.3",
    "rimraf": "^6.0.1",
    "sort-package-json": "^3.6.1",
    "tsx": "^4.21.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.6"
  },
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=22"
  },
  "publishConfig": {
    "access": "public",
    "provenance": true
  }
}
