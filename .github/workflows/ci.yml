name: CI

on:
  pull_request:
    # `edited` is included so the changelog-completeness gate re-runs when a
    # title is corrected — the PR title is the release-please bump signal
    # (SK-379), so a stale pass on an old title would be unsafe. The
    # conventional-PR-title lint lives in validate-pr-title.yml (A-431) and
    # re-runs on `edited` there too.
    types: [opened, edited, synchronize, reopened]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

# Defence-in-depth (ASW-313): the repo default is already read-only, but
# pin it here so a future org-policy change can't silently widen CI's token.
permissions:
  contents: read

# Wave 3 consumer (A-589 / A-593): lint + build-test run through the estate's
# shared reusable workflows, SHA-pinned to v1.0.0 so Dependabot bumps them
# together. This repo is config-only — no build step and no ESLint (its
# published artefact is `.markdownlint.jsonc`), so the lint job runs markdownlint
# + YAML/actionlint + changelog validation, and build-test runs typecheck +
# Vitest + ShellCheck + bats with `build: false`. The changelog-completeness gate
# and the `GO/NO GO` aggregator stay local (A-418). PR-title validation is a
# separate workflow (validate-pr-title.yml, A-431) required directly by the
# estate ruleset.
jobs:
  config:
    name: ⚙️ Load repo config
    runs-on: ubuntu-latest
    outputs:
      node_version_file: ${{ steps.repo.outputs.node_version_file }}
    steps:
      - name: ⬇️ Checkout repo
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 1
          persist-credentials: false

      - name: ⚙️ Load repo config
        id: repo
        uses: ./.github/actions/load-repo-config

  lint:
    needs: config
    uses: acme-skunkworks/shared-workflows/.github/workflows/reusable-lint.yml@9febdb14373f383e3ae4f5a6e3f6bb75a59d0c3d # v1.0.2
    with:
      node-version-file: ${{ needs.config.outputs.node_version_file }}
      # Config-only package: no TypeScript/JS source to ESLint.
      eslint: false
      # Mirror the repo's own `lint:md` script so CI lints exactly what the repo
      # intends — vendored skill bundles (.claude/skills, .agents/skills) and
      # build output are excluded.
      markdown-globs: |
        **/*.{md,mdx}
        !**/node_modules/**
        !**/dist/**
        !**/.turbo/**
        !**/.astro/**
        !.agents/skills/**
        !.claude/skills/**
      changelog-script: validate:changelog

  build-test:
    needs: config
    uses: acme-skunkworks/shared-workflows/.github/workflows/reusable-build-test.yml@9febdb14373f383e3ae4f5a6e3f6bb75a59d0c3d # v1.0.2
    with:
      node-version-file: ${{ needs.config.outputs.node_version_file }}
      # No build step — the published artefact is `.markdownlint.jsonc` itself.
      build: false
      shellcheck-paths: "infrastructure/scripts .husky/pre-commit .husky/pre-push .husky/commit-msg"
      bats: true

  changelog-completeness:
    name: 📓 Changelog completeness
    needs: config
    runs-on: ubuntu-latest
    env:
      HUSKY: 0
    steps:
      - name: ⬇️ Checkout repo
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: 📦 Install pnpm
        uses: pnpm/action-setup@0ebf47130e4866e96fce0953f49152a61190b271 # v4.4.0
        with:
          run_install: false

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version-file: ${{ needs.config.outputs.node_version_file }}
          cache: pnpm

      - name: 📥 Install dependencies
        run: pnpm install --frozen-lockfile

      - name: 📓 Changelog completeness
        env:
          PR_TITLE: ${{ github.event.pull_request.title }}
          BASE_REF: ${{ github.base_ref }}
        run: pnpm tsx infrastructure/scripts/check-changelog-completeness.ts

  go-no-go:
    name: GO/NO GO
    needs: [config, lint, build-test, changelog-completeness]
    if: ${{ always() }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: ⚖️ Verdict
        env:
          NEEDS_JSON: ${{ toJSON(needs) }}
        run: |
          set -euo pipefail
          echo "$NEEDS_JSON" \
            | jq -e 'to_entries | all(.value.result == "success" or .value.result == "skipped")' >/dev/null \
            || { echo "::error::a required check failed — refusing to go"; exit 1; }
          echo "All required checks passed — GO."
